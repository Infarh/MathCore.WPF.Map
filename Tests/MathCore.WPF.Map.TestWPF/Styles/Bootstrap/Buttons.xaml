<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="Colors.xaml"/>
    </ResourceDictionary.MergedDictionaries>
    
    <!-- ������� ����� ������ Bootstrap -->
    <Style x:Key="Button.Base" TargetType="Button">
        <Setter Property="FontFamily" Value="Segoe UI"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="FontWeight" Value="Normal"/>
        <Setter Property="Padding" Value="12,6"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="4"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                        VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Opacity" Value="0.8"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="border" Property="Opacity" Value="0.65"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
    <!-- Primary Button -->
    <Style x:Key="Button.Primary" TargetType="Button" BasedOn="{StaticResource Button.Base}">
        <Setter Property="Background" Value="{StaticResource Brush.Primary}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Primary}"/>
        <Setter Property="Foreground" Value="{StaticResource Brush.White}"/>
    </Style>
    
    <!-- Secondary Button -->
    <Style x:Key="Button.Secondary" TargetType="Button" BasedOn="{StaticResource Button.Base}">
        <Setter Property="Background" Value="{StaticResource Brush.Secondary}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Secondary}"/>
        <Setter Property="Foreground" Value="{StaticResource Brush.White}"/>
    </Style>
    
    <!-- Success Button -->
    <Style x:Key="Button.Success" TargetType="Button" BasedOn="{StaticResource Button.Base}">
        <Setter Property="Background" Value="{StaticResource Brush.Success}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Success}"/>
        <Setter Property="Foreground" Value="{StaticResource Brush.White}"/>
    </Style>
    
    <!-- Danger Button -->
    <Style x:Key="Button.Danger" TargetType="Button" BasedOn="{StaticResource Button.Base}">
        <Setter Property="Background" Value="{StaticResource Brush.Danger}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Danger}"/>
        <Setter Property="Foreground" Value="{StaticResource Brush.White}"/>
    </Style>
    
    <!-- Warning Button -->
    <Style x:Key="Button.Warning" TargetType="Button" BasedOn="{StaticResource Button.Base}">
        <Setter Property="Background" Value="{StaticResource Brush.Warning}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Warning}"/>
        <Setter Property="Foreground" Value="{StaticResource Brush.Dark}"/>
    </Style>
    
    <!-- Info Button -->
    <Style x:Key="Button.Info" TargetType="Button" BasedOn="{StaticResource Button.Base}">
        <Setter Property="Background" Value="{StaticResource Brush.Info}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Info}"/>
        <Setter Property="Foreground" Value="{StaticResource Brush.Dark}"/>
    </Style>
    
    <!-- Light Button -->
    <Style x:Key="Button.Light" TargetType="Button" BasedOn="{StaticResource Button.Base}">
        <Setter Property="Background" Value="{StaticResource Brush.Light}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Light}"/>
        <Setter Property="Foreground" Value="{StaticResource Brush.Dark}"/>
    </Style>
    
    <!-- Dark Button -->
    <Style x:Key="Button.Dark" TargetType="Button" BasedOn="{StaticResource Button.Base}">
        <Setter Property="Background" Value="{StaticResource Brush.Dark}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Dark}"/>
        <Setter Property="Foreground" Value="{StaticResource Brush.White}"/>
    </Style>
    
    <!-- Outline Buttons (���������) -->
    <Style x:Key="Button.Outline.Base" TargetType="Button">
        <Setter Property="FontFamily" Value="Segoe UI"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="FontWeight" Value="Normal"/>
        <Setter Property="Padding" Value="12,6"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="4"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter x:Name="content"
                                        HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                        VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{Binding BorderBrush, RelativeSource={RelativeSource TemplatedParent}}"/>
                            <Setter Property="Foreground" Value="{StaticResource Brush.White}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="border" Property="Opacity" Value="0.65"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
    <Style x:Key="Button.Outline.Primary" TargetType="Button" BasedOn="{StaticResource Button.Outline.Base}">
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Primary}"/>
        <Setter Property="Foreground" Value="{StaticResource Brush.Primary}"/>
    </Style>
    
    <Style x:Key="Button.Outline.Secondary" TargetType="Button" BasedOn="{StaticResource Button.Outline.Base}">
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Secondary}"/>
        <Setter Property="Foreground" Value="{StaticResource Brush.Secondary}"/>
    </Style>
    
    <Style x:Key="Button.Outline.Success" TargetType="Button" BasedOn="{StaticResource Button.Outline.Base}">
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Success}"/>
        <Setter Property="Foreground" Value="{StaticResource Brush.Success}"/>
    </Style>
    
    <Style x:Key="Button.Outline.Danger" TargetType="Button" BasedOn="{StaticResource Button.Outline.Base}">
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Danger}"/>
        <Setter Property="Foreground" Value="{StaticResource Brush.Danger}"/>
    </Style>
    
    <Style x:Key="Button.Outline.Warning" TargetType="Button" BasedOn="{StaticResource Button.Outline.Base}">
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Warning}"/>
        <Setter Property="Foreground" Value="{StaticResource Brush.Warning}"/>
    </Style>
    
    <Style x:Key="Button.Outline.Info" TargetType="Button" BasedOn="{StaticResource Button.Outline.Base}">
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Info}"/>
        <Setter Property="Foreground" Value="{StaticResource Brush.Info}"/>
    </Style>
    
    <Style x:Key="Button.Outline.Light" TargetType="Button" BasedOn="{StaticResource Button.Outline.Base}">
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Light}"/>
        <Setter Property="Foreground" Value="{StaticResource Brush.Light}"/>
    </Style>
    
    <Style x:Key="Button.Outline.Dark" TargetType="Button" BasedOn="{StaticResource Button.Outline.Base}">
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Dark}"/>
        <Setter Property="Foreground" Value="{StaticResource Brush.Dark}"/>
    </Style>
    
    <!-- ������� ������ -->
    <Style x:Key="Button.Small" TargetType="Button">
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Padding" Value="8,4"/>
    </Style>
    
    <Style x:Key="Button.Large" TargetType="Button">
        <Setter Property="FontSize" Value="18"/>
        <Setter Property="Padding" Value="16,10"/>
    </Style>
    
    <!-- Link Button (��� ������) -->
    <Style x:Key="Button.Link" TargetType="Button">
        <Setter Property="FontFamily" Value="Segoe UI"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Foreground" Value="{StaticResource Brush.Primary}"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <TextBlock x:Name="text"
                             Text="{TemplateBinding Content}"
                             Foreground="{TemplateBinding Foreground}"
                             TextDecorations="Underline"
                             Background="Transparent"/>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="text" Property="Foreground" Value="{StaticResource Brush.Blue.700}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="text" Property="Foreground" Value="{StaticResource Brush.Gray.600}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
    <!-- ������� ������ (�� ��� ������) -->
    <Style x:Key="Button.Block" TargetType="Button">
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="Width" Value="Auto"/>
    </Style>
    
</ResourceDictionary>
